#!/bin/bash
#
# This silly thing exists because URxvt.urlLauncher (from .Xdefaults) has to
# be a single-word parameter. i.e., "run-chrome -e sel" won't work because it
# needs args.

urxvt -bl -geometry 100x10+5+5 -e run-chrome sel $1
