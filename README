This directory contains lots of dotfiles. It's all shell environment
and window manager customization type stuff.

See also: http://wiki.eater.org/ocd

Running ocd-update will push everything here to your $HOME. It will back
up your existing crap in a tarball.

Running ocd-downdate will copy your current files back into $HOME/.ocd
where you may commit them back to the git repository.
